{
  "name": "maisoncbdvape-eleventy",
  "version": "1.0.0",
  "description": "MaisonCBDVape — Site e-commerce JAMstack Eleventy + Cloudflare",
  "type": "module",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('public',{recursive:true,force:true})\"",
    "css": "tailwindcss -i ./tailwind/input.css -o ./src/assets/css/tailwind.css --minify",
    "catalog": "node scripts/build-catalog-index.js",
    "build": "npm run clean && npm run css && npm run catalog && eleventy",
    "start": "concurrently \"npm run css -- --watch\" \"eleventy --serve --incremental\"",
    "dev": "wrangler pages dev public --kv ORDERS_KV --kv OAUTH_KV --r2 MEDIA",
    "deploy": "npm run build && wrangler pages deploy public"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "concurrently": "^8.2.2",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
