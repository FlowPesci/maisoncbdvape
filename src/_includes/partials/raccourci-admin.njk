{# ───────────────────────────────────────────────────────────────────────────
   Raccourci vers le back-office, sur toutes les pages de la boutique.

   Il n'apparaît que si une session d'administration existe déjà côté serveur
   — donc seulement pour le commerçant, et seulement sur l'appareil où il
   s'est connecté. Le cookie `mcv_admin_hint` ne porte aucun secret : c'est
   un simple indicateur de présence, posé à côté du cookie de session
   HttpOnly (functions/_shared/session.js). Le jeton GitHub lui-même n'est
   jamais lu ici.

   ⚠ Le bouton est construit en JavaScript, jamais écrit dans le HTML livré :
   un client qui affiche le code source ou un robot d'indexation n'y trouve
   aucune trace du back-office. C'est la raison d'être de ce fichier ; ne pas
   le « simplifier » en un lien statique masqué en CSS, qui serait visible de
   tous dans le source.

   Ce n'est qu'un raccourci d'affichage : chaque appel à l'API revérifie la
   session côté serveur. Il ne donne aucun droit.
   ─────────────────────────────────────────────────────────────────────── #}

<script src="/assets/js/raccourci-admin.js" defer></script>
